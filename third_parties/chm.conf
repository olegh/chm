#
# start country house monitor
#

description "County House Monitor service"

start on (started wvdial)
console output

env HOME=/root

pre-start script
  /opt/owfs/bin/owfs --allow_other -u /media/1-wire
  /opt/chm/script/hamachi.pl
  /opt/chm/bin/auto_updater -d &
  /opt/chm/bin/watchdog -d -c /media/1-wire/81.F0002D000000 &
end script


exec /opt/chm/bin/chm -d -m snail.incm@gmail.com
respawn
